-- آیدی نسخه/دوره اکانت تستی که کاربر آخرین بار از آن استفاده کرده (برای نمایش به ادمین و پاک‌سازی مجزا)
ALTER TABLE users ADD COLUMN last_test_id INTEGER;

-- آیدی جشنواره‌ای که کاربر آخرین بار از آن استفاده کرده
-- (هر جشنواره فقط با همین آیدی یک‌بار برای همیشه توسط هر کاربر قابل استفاده است؛
--  با شروع جشنواره جدید توسط ادمین، آیدی جشنواره در تنظیمات عوض می‌شود و همه کاربران دوباره مجاز می‌شوند)
ALTER TABLE users ADD COLUMN last_festival_id INTEGER;
